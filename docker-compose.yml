services:
  postgres:
    image: postgres:17
    container_name: breakground-postgres
    environment:
      POSTGRES_DB: breakground
      POSTGRES_USER: breakground
      POSTGRES_PASSWORD: breakground
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: